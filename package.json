{
  "name": "example-extension-checkout",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@shopify/checkout-ui-extensions-react": "^0.27.3"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "ciniu",
  "private": true,
  "workspaces": [
    "extensions/*"
  ]
}
